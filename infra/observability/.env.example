# Copy to .env on the staging Droplet (never commit real secrets).

GRAFANA_ADMIN_USER=admin
GRAFANA_ADMIN_PASSWORD=change-me
VECINITA_GRAFANA_URL=http://127.0.0.1:3000
VECINITA_ALERTMANAGER_WEBHOOK_URL=https://example.invalid/staging-webhook
