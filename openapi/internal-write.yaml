openapi: 3.0.3
info:
  title: Vecinita DO Internal Write API
  version: 0.2.0
  description: Service-to-service writes from Modal workers (sole DATABASE_URL holder).
servers:
  - url: /internal/v1
    description: Internal write API (DigitalOcean)
paths:
  /documents/batch:
    post:
      operationId: upsertDocumentsBatch
      summary: Upsert documents, chunks, and embeddings
      security:
        - internalApiKey: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/BatchUpsertRequest"
      responses:
        "200":
          description: Upsert counts
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BatchUpsertResponse"
        "400":
          $ref: "#/components/responses/ValidationError"
        "401":
          $ref: "#/components/responses/Unauthorized"
  /documents:
    get:
      operationId: listDocuments
      summary: List corpus documents
      security:
        - internalApiKey: []
      responses:
        "200":
          description: Document list
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/DocumentSummary"
        "401":
          $ref: "#/components/responses/Unauthorized"
  /documents/{document_id}:
    delete:
      operationId: deleteDocument
      summary: Delete document and dependent chunks/embeddings
      security:
        - internalApiKey: []
      parameters:
        - name: document_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "204":
          description: Deleted
        "404":
          $ref: "#/components/responses/NotFound"
        "401":
          $ref: "#/components/responses/Unauthorized"
  /documents/{document_id}/chunks:
    get:
      operationId: listDocumentChunks
      summary: List chunks for admin viewer
      security:
        - internalApiKey: []
      parameters:
        - name: document_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Chunk list with tags
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/ChunkDetail"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
  /documents/{document_id}/tags:
    patch:
      operationId: patchDocumentTags
      summary: Replace document tags (human or llm batch)
      security:
        - internalApiKey: []
      parameters:
        - name: document_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/TagPatchRequest"
      responses:
        "200":
          description: Updated tags
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TagPatchResponse"
        "400":
          $ref: "#/components/responses/ValidationError"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
  /documents/{document_id}/retag:
    post:
      operationId: retagDocumentLlm
      summary: Re-run LLM tagging for a document
      security:
        - internalApiKey: []
      parameters:
        - name: document_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Updated tags from LLM
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TagPatchResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
  /chunks/{chunk_id}/tags:
    patch:
      operationId: patchChunkTags
      summary: Replace chunk-level tags
      security:
        - internalApiKey: []
      parameters:
        - name: chunk_id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/TagPatchRequest"
      responses:
        "200":
          description: Updated chunk tags
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TagPatchResponse"
        "400":
          $ref: "#/components/responses/ValidationError"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
  /health:
    get:
      operationId: health
      summary: Service health
      responses:
        "200":
          description: Health status
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HealthResponse"
components:
  securitySchemes:
    internalApiKey:
      type: http
      scheme: bearer
      bearerFormat: API key
      description: VECINITA_INTERNAL_API_KEY
  schemas:
    EmbeddingVector:
      type: array
      minItems: 384
      maxItems: 384
      items:
        type: number
        format: float
    ChunkUpsert:
      type: object
      required:
        - chunk_index
        - text
        - embedding
      properties:
        chunk_index:
          type: integer
          minimum: 0
        text:
          type: string
        embedding:
          $ref: "#/components/schemas/EmbeddingVector"
    DocumentUpsert:
      type: object
      required:
        - url
        - chunks
      properties:
        url:
          type: string
          format: uri
        title:
          type: string
          nullable: true
        content_hash:
          type: string
          nullable: true
        language:
          type: string
          nullable: true
        chunks:
          type: array
          minItems: 1
          items:
            $ref: "#/components/schemas/ChunkUpsert"
        tags:
          type: array
          maxItems: 10
          items:
            $ref: "#/components/schemas/TagInput"
    TagInput:
      type: object
      required:
        - slug
        - label
      properties:
        slug:
          type: string
        label:
          type: string
        source:
          type: string
          enum:
            - llm
            - human
    TagPatchRequest:
      type: object
      required:
        - tags
        - source
      properties:
        tags:
          type: array
          maxItems: 10
          items:
            $ref: "#/components/schemas/TagInput"
        source:
          type: string
          enum:
            - llm
            - human
    TagPatchResponse:
      type: object
      required:
        - tags
      properties:
        tags:
          type: array
          items:
            $ref: "#/components/schemas/TagInput"
    ChunkDetail:
      type: object
      required:
        - chunk_id
        - chunk_index
        - text
      properties:
        chunk_id:
          type: string
          format: uuid
        chunk_index:
          type: integer
        text:
          type: string
        token_count:
          type: integer
          nullable: true
        tags:
          type: array
          items:
            $ref: "#/components/schemas/TagInput"
    BatchUpsertRequest:
      type: object
      additionalProperties: false
      required:
        - documents
      properties:
        documents:
          type: array
          minItems: 1
          items:
            $ref: "#/components/schemas/DocumentUpsert"
    BatchUpsertResponse:
      type: object
      required:
        - upserted_chunks
      properties:
        upserted_chunks:
          type: integer
          minimum: 0
    DocumentSummary:
      type: object
      required:
        - document_id
        - url
      properties:
        document_id:
          type: string
          format: uuid
        url:
          type: string
        title:
          type: string
          nullable: true
        language:
          type: string
          nullable: true
        tags:
          type: array
          items:
            $ref: "#/components/schemas/TagInput"
    HealthResponse:
      type: object
      required:
        - status
      properties:
        status:
          type: string
          enum:
            - ok
    ErrorBody:
      type: object
      properties:
        detail:
          type: string
  responses:
    ValidationError:
      description: Invalid payload
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorBody"
    Unauthorized:
      description: Missing or invalid internal API key
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorBody"
    NotFound:
      description: Unknown document
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ErrorBody"
