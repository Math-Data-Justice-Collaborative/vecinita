# T80.7 operator smoke — 2026-07-24

> Session: S010-unify-llm-service · AC-E38 / Phase 18 Slice D  
> Branch: `feat/S010-unify-llm-service`

## Commands run

1. `bash scripts/deploy/sync_llm_secret.sh --apply` — OK (`vecinita-llm` secret)
2. `bash scripts/deploy/modal.sh` — embedding + data-mgmt OK; LLM failed on bare `modal` PATH (no workspace packages). Redeployed with `uv run modal` (script patched).
3. DO: wrote `VECINITA_MODAL_LLM_PLAYGROUND_URL=https://vecinita--vecinita-llm-playground-fastapi-app.modal.run` into `prod.env`; `do_apps.py sync-all-secrets`; redeployed `vecinita-internal-write-api`; `sync_github_secrets.sh --apply` (new GH secret).
4. `do_verify_required_secrets.sh` — OK (ChatRAG `modal_llm=ok`)

## Blockers found + fixed (live)

| Issue | Symptom | Fix |
|-------|---------|-----|
| Bare `modal` on PATH | `ModuleNotFoundError: vecinita_shared_schemas` at deploy | `scripts/deploy/modal.sh` prefers `uv run modal` |
| Eager package `__init__` | Playground (and new prod containers) crash-loop: `No module named 'jwt'` | Lazy `vecinita_shared_schemas.__init__` + AST regression test |
| ASGI without volume | Pull `202` then never `available`; `Volume.commit` ConflictError | Mount `llm-models` on both `fastapi_app`s; commit via `Volume.from_name(VOLUME_NAME)` |

## Smoke results (PASS)

| Check | Result |
|-------|--------|
| Prod `/health` | 200 |
| Playground `/health` | 200 (after jwt fix) |
| Playground `POST /models/ollama/pull` `qwen2.5:1.5b-instruct` | 202 → list `available: true` |
| Prod `generate` before/after playground warm | OK (`PROD_OK` / `PROD_STILL_OK`) |
| ChatRAG `/health` | `dependencies.modal_llm=ok` (unaffected) |

## URLs

| App | URL |
|-----|-----|
| Prod LLM | `https://vecinita--vecinita-llm-fastapi-app.modal.run` |
| Playground LLM | `https://vecinita--vecinita-llm-playground-fastapi-app.modal.run` |

## Status

**T80.7: completed** (operator live deploy + smoke). Uncommitted deploy fixes on branch pending commit/push for PR-53.
