name: CI - Chat Frontend
on:
  push:
    branches: [main]
    paths: ['apps/chat-frontend/**']
  pull_request:
    paths: ['apps/chat-frontend/**']
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: { node-version: '20' }
      - run: cd apps/chat-frontend && npm ci
      - run: cd apps/chat-frontend && npm run lint
      - run: cd apps/chat-frontend && npm test -- --run
