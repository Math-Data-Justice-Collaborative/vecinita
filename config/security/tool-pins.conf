# Pinned security scanner release tags for scripts/security/install-tools.sh
# (#227 / BUG-2026-08-06 Phase 5). Prefer direct download URLs over /releases/latest.
# Bump intentionally after verifying assets exist; set SEC_TOOLS_UNPIN=1 to use API/latest.
# Grype/OpenGrep use vendor installers (not pinned here).

SEC_PIN_2MS_TAG=v5.2.4
SEC_PIN_KICS_TAG=v2.1.20
SEC_PIN_SBOM_TOOL_TAG=v4.1.5
