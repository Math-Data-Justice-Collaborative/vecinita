[project]
name = "vecinita-rag"
version = "0.1.0"
description = "Shared LlamaIndex RAG pipelines"
requires-python = ">=3.11,<3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["vecinita_rag"]
