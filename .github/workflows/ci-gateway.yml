name: CI - Gateway
on:
  push:
    branches: [main]
    paths: ['apps/gateway/**', 'packages/**']
  pull_request:
    paths: ['apps/gateway/**', 'packages/**']
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: { python-version: '3.11' }
      - run: pip install uv && cd apps/gateway && uv sync
      - run: cd apps/gateway && uv run ruff check src/
      - run: cd apps/gateway && uv run pytest tests/ -x --timeout=60
