# Render / local builds with dockerContext: ./backend (see specs/016 T013).
# Keep runtime markdown under src/ (e.g. system_rules.md, FAQs).

tests/
**/__pycache__/
**/*.pyc
.pytest_cache/
htmlcov/
.coverage
.git
.venv/
venv/
*.egg-info/
dist/
build/

# Broad markdown ignore + explicit keep for shipped agent data (cannot exclude src/**/*.md).
**/*.md
!src/**/*.md
