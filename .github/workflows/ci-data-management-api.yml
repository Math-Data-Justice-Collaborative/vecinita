name: CI - Data Management API
on:
  push:
    branches: [main]
    paths: ['apps/data-management-api/**', 'packages/**']
  pull_request:
    paths: ['apps/data-management-api/**', 'packages/**']
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: { python-version: '3.11' }
      - run: pip install uv && cd apps/data-management-api && uv sync
      - run: cd apps/data-management-api && uv run ruff check src/
      - run: cd apps/data-management-api && uv run pytest tests/ -x --timeout=60
