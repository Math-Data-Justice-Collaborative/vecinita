[project]
name = "vecinita-indexing-worker"
version = "0.1.0"
description = "Document indexing pipeline: single-doc, batch, selective re-index, full rebuild"
requires-python = ">=3.11"
dependencies = [
    "modal>=1.3.5",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
