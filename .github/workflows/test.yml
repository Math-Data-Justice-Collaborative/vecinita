name: Tests

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]
  workflow_dispatch:

concurrency:
  group: tests-${{ github.ref }}
  cancel-in-progress: true

jobs:
  workflow-context:
    runs-on: ubuntu-22.04
    outputs:
      has_cross_repo_token: ${{ steps.detect.outputs.has_cross_repo_token }}
    steps:
    - name: Detect cross-repo token availability
      id: detect
      env:
        HAS_CROSS_REPO_TOKEN: ${{ secrets.CROSS_REPO_WORKFLOW_TOKEN != '' }}
      run: echo "has_cross_repo_token=${HAS_CROSS_REPO_TOKEN}" >> "$GITHUB_OUTPUT"

  secret-scan:
    runs-on: ubuntu-22.04
    permissions:
      contents: read
    steps:
    - name: Checkout code
      uses: actions/checkout@v5
      with:
        fetch-depth: 0
        submodules: 'false'

    - name: Install gitleaks
      run: |
        set -euo pipefail
        version="8.24.2"
        curl -sSL "https://github.com/gitleaks/gitleaks/releases/download/v${version}/gitleaks_${version}_linux_x64.tar.gz" | tar -xz
        sudo install gitleaks /usr/local/bin/gitleaks

    - name: Run gitleaks
      run: |
        if ! gitleaks dir . --redact --config .gitleaks.toml; then
          echo "::warning::gitleaks found potential secrets. Review findings and tighten .gitleaks.toml allowlists as needed."
        fi

  backend-quality:
    runs-on: ubuntu-22.04
    steps:
    - name: Checkout code
      uses: actions/checkout@v5
      with:
        submodules: 'false'

    - name: Checkout contract test submodules
      run: git submodule update --init --depth 1 services/embedding-modal services/model-modal

    - name: Set up Python 3.10
      uses: actions/setup-python@v6
      with:
        python-version: "3.10"
        cache: 'pip'

    - name: Install backend quality dependencies
      working-directory: backend
      run: |
        python -m pip install --upgrade pip
        pip install --no-cache-dir ".[ci]"

    - name: Lint backend (Ruff)
      working-directory: backend
      run: python -m ruff check src tests

    - name: Check backend formatting (Black)
      working-directory: backend
      run: python -m black --check src tests

    - name: Type check backend (mypy)
      working-directory: backend
      run: python -m mypy src

  frontend-quality:
    runs-on: ubuntu-22.04
    steps:
    - name: Checkout code
      uses: actions/checkout@v5
      with:
        submodules: 'false'

    - name: Checkout frontend submodule
      run: git submodule update --init --depth 1 frontend

    - name: Set up Node.js
      uses: actions/setup-node@v5
      with:
        node-version: '20'
        cache: 'npm'
        cache-dependency-path: frontend/package-lock.json

    - name: Install frontend dependencies
      working-directory: frontend
      run: npm ci

    - name: Lint frontend
      working-directory: frontend
      run: npm run lint

    - name: Check frontend formatting (Prettier)
      working-directory: frontend
      run: npm run format

    - name: Type check frontend
      working-directory: frontend
      run: npm run typecheck

  data-management-api-structure:
    needs: [workflow-context]
    if: needs.workflow-context.outputs.has_cross_repo_token == 'true'
    runs-on: ubuntu-22.04
    steps:
    - name: Checkout code
      uses: actions/checkout@v5
      with:
        submodules: 'false'
        token: ${{ secrets.CROSS_REPO_WORKFLOW_TOKEN }}

    - name: Checkout data-management API submodule
      run: git submodule update --init --depth 1 services/data-management-api

    - name: Validate imported data-management repo structure
      run: |
        test -f services/data-management-api/.gitmodules
        test -d services/data-management-api/apps/backend
        test -d services/data-management-api/apps/frontend

  data-management-frontend-quality:
    needs: [workflow-context]
    if: needs.workflow-context.outputs.has_cross_repo_token == 'true'
    runs-on: ubuntu-22.04
    steps:
    - name: Checkout code
      uses: actions/checkout@v5
      with:
        submodules: 'false'
        token: ${{ secrets.CROSS_REPO_WORKFLOW_TOKEN }}

    - name: Checkout data-management frontend submodule
      run: git submodule update --init --depth 1 apps/data-management-frontend

    - name: Set up Node.js
      uses: actions/setup-node@v5
      with:
        node-version: '20'
        cache: 'npm'
        cache-dependency-path: apps/data-management-frontend/package-lock.json

    - name: Install data-management frontend dependencies
      working-directory: apps/data-management-frontend
      run: npm ci

    - name: Lint data-management frontend
      working-directory: apps/data-management-frontend
      run: npm run lint

    - name: Build data-management frontend
      working-directory: apps/data-management-frontend
      run: npm run build

  data-management-frontend-ci:
    needs: [workflow-context]
    if: needs.workflow-context.outputs.has_cross_repo_token == 'true'
    runs-on: ubuntu-22.04
    steps:
    - name: Checkout code
      uses: actions/checkout@v5
      with:
        submodules: 'false'
        token: ${{ secrets.CROSS_REPO_WORKFLOW_TOKEN }}

    - name: Checkout data-management frontend submodule
      run: git submodule update --init --depth 1 apps/data-management-frontend

    - name: Set up Node.js
      uses: actions/setup-node@v5
      with:
        node-version: '20'
        cache: 'npm'
        cache-dependency-path: apps/data-management-frontend/package-lock.json

    - name: Install data-management frontend dependencies
      working-directory: apps/data-management-frontend
      run: npm ci

    - name: Run data-management frontend tests
      working-directory: apps/data-management-frontend
      run: npm run test

  embedding-modal-quality:
    runs-on: ubuntu-22.04
    steps:
    - name: Checkout code
      uses: actions/checkout@v5
      with:
        submodules: 'false'

    - name: Checkout embedding-modal submodule
      run: git submodule update --init --depth 1 services/embedding-modal

    - name: Set up Python 3.11
      uses: actions/setup-python@v6
      with:
        python-version: '3.11'
        cache: 'pip'

    - name: Install embedding-modal dependencies
      working-directory: services/embedding-modal
      run: |
        python -m pip install --upgrade pip
        python -m pip install --upgrade setuptools wheel
        pip install ".[dev]"

    - name: Lint embedding-modal
      working-directory: services/embedding-modal
      run: make lint

  embedding-modal-ci:
    runs-on: ubuntu-22.04
    steps:
    - name: Checkout code
      uses: actions/checkout@v5
      with:
        submodules: 'false'

    - name: Checkout embedding-modal submodule
      run: git submodule update --init --depth 1 services/embedding-modal

    - name: Set up Python 3.11
      uses: actions/setup-python@v6
      with:
        python-version: '3.11'
        cache: 'pip'

    - name: Install embedding-modal dependencies
      working-directory: services/embedding-modal
      run: |
        python -m pip install --upgrade pip
        python -m pip install --upgrade setuptools wheel
        pip install ".[dev]"

    - name: Run embedding-modal tests
      working-directory: services/embedding-modal
      run: make test

  model-modal-quality:
    runs-on: ubuntu-22.04
    steps:
    - name: Checkout code
      uses: actions/checkout@v5
      with:
        submodules: 'false'

    - name: Checkout model-modal submodule
      run: git submodule update --init --depth 1 services/model-modal

    - name: Set up Python 3.11
      uses: actions/setup-python@v6
      with:
        python-version: '3.11'
        cache: 'pip'

    - name: Install model-modal dependencies
      working-directory: services/model-modal
      run: |
        python -m pip install --upgrade pip
        python -m pip install --upgrade setuptools wheel
        pip install ".[dev]"

    - name: Lint model-modal
      working-directory: services/model-modal
      run: make lint

  model-modal-ci:
    runs-on: ubuntu-22.04
    steps:
    - name: Checkout code
      uses: actions/checkout@v5
      with:
        submodules: 'false'

    - name: Checkout model-modal submodule
      run: git submodule update --init --depth 1 services/model-modal

    - name: Set up Python 3.11
      uses: actions/setup-python@v6
      with:
        python-version: '3.11'
        cache: 'pip'

    - name: Install model-modal dependencies
      working-directory: services/model-modal
      run: |
        python -m pip install --upgrade pip
        python -m pip install --upgrade setuptools wheel
        pip install ".[dev]"

    - name: Run model-modal tests
      working-directory: services/model-modal
      run: make test

  scraper-quality:
    needs: [workflow-context]
    if: needs.workflow-context.outputs.has_cross_repo_token == 'true'
    runs-on: ubuntu-22.04
    steps:
    - name: Checkout code
      uses: actions/checkout@v5
      with:
        submodules: 'false'
        token: ${{ secrets.CROSS_REPO_WORKFLOW_TOKEN }}

    - name: Checkout scraper submodule
      run: git submodule update --init --depth 1 services/scraper

    - name: Set up Python 3.11
      uses: actions/setup-python@v6
      with:
        python-version: '3.11'
        cache: 'pip'

    - name: Install scraper dependencies
      working-directory: services/scraper
      run: |
        python -m pip install --upgrade pip
        pip install --no-cache-dir -e ".[dev]"

    - name: Lint scraper
      working-directory: services/scraper
      run: make lint

    - name: Check scraper formatting
      working-directory: services/scraper
      run: black --check src/ tests/

    - name: Type check scraper
      working-directory: services/scraper
      run: make type-check

  scraper-ci:
    needs: [workflow-context]
    if: needs.workflow-context.outputs.has_cross_repo_token == 'true'
    runs-on: ubuntu-22.04
    steps:
    - name: Checkout code
      uses: actions/checkout@v5
      with:
        submodules: 'false'
        token: ${{ secrets.CROSS_REPO_WORKFLOW_TOKEN }}

    - name: Checkout scraper submodule
      run: git submodule update --init --depth 1 services/scraper

    - name: Set up Python 3.11
      uses: actions/setup-python@v6
      with:
        python-version: '3.11'
        cache: 'pip'

    - name: Install scraper dependencies
      working-directory: services/scraper
      run: |
        python -m pip install --upgrade pip
        pip install --no-cache-dir -e ".[dev]"

    - name: Run scraper tests
      working-directory: services/scraper
      run: make test

  backend-ci:
    runs-on: ubuntu-22.04

    steps:
    - name: Checkout code
      uses: actions/checkout@v5
      with:
        submodules: 'false'

    - name: Checkout contract test submodules
      run: git submodule update --init --depth 1 services/embedding-modal services/model-modal

    - name: Free disk space
      run: |
        sudo rm -rf /usr/share/dotnet
        sudo rm -rf /opt/ghc
        sudo rm -rf /usr/local/lib/android
        sudo rm -rf /opt/hostedtoolcache
        sudo apt-get clean
        sudo rm -rf /var/lib/apt/lists/*
        docker system prune -af || true

    - name: Set up Python 3.10
      uses: actions/setup-python@v6
      with:
        python-version: "3.10"
        cache: 'pip'

    - name: Install system dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y --no-install-recommends \
          build-essential \
          graphviz \
          graphviz-dev \
          pkg-config

    - name: Install Python dependencies (lean CI)
      working-directory: backend
      run: |
        python -m pip install --upgrade pip
        pip install --no-cache-dir ".[ci]"

    # Skip Playwright browser install for non-UI tests to save space
    - name: Skip Playwright browsers (no UI tests)
      run: echo "Playwright browser install skipped in CI"

    - name: Create .env file for tests
      working-directory: backend
      run: |
        echo "DATABASE_URL=${{ secrets.DATABASE_URL || 'postgresql://test' }}" >> .env
        echo "OLLAMA_BASE_URL=http://localhost:11434" >> .env
        echo "OLLAMA_MODEL=llama3.1:8b" >> .env
        echo "USE_LOCAL_EMBEDDINGS=true" >> .env
        echo "EMBEDDING_MODEL=sentence-transformers/all-MiniLM-L6-v2" >> .env
        echo "EMBEDDING_SERVICE_AUTH_TOKEN=test-embed-token" >> .env
        echo "EMBEDDING_STRICT_STARTUP=false" >> .env
        echo "REINDEX_TRIGGER_TOKEN=test-reindex-token" >> .env
        echo "REINDEX_SERVICE_URL=https://example.modal.run" >> .env
      shell: bash

    - name: Run backend fast tests
      working-directory: backend
      run: |
        pytest tests/ -m "not integration and not e2e and not llm" -v --tb=short --junit-xml=test-results.xml \
          --cov=src.embedding_service.modal_app \
          --cov=src.services.embedding.models \
          --cov=src.utils.tags \
          --cov-config=.coveragerc.unit --cov-report=xml --cov-report=html --cov-report=term \
          --cov-fail-under=98
      env:
        PYTHONPATH: ${{ github.workspace }}/backend
        SKIP_AGENT_MAIN_IMPORT: 'true'
        EMBEDDING_STRICT_STARTUP: 'false'

    - name: API regression - ask config returns JSON
      working-directory: backend
      run: |
        pytest tests/test_api/test_gateway_router_ask.py -k "get_ask_config_success" -v --tb=short
      env:
        PYTHONPATH: ${{ github.workspace }}/backend
        EMBEDDING_STRICT_STARTUP: 'false'

  backend-integration:
    runs-on: ubuntu-22.04

    steps:
    - name: Checkout code
      uses: actions/checkout@v5
      with:
        submodules: 'false'

    - name: Set up Python 3.10
      uses: actions/setup-python@v6
      with:
        python-version: "3.10"
        cache: 'pip'

    - name: Install system dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y --no-install-recommends \
          build-essential \
          graphviz \
          graphviz-dev \
          pkg-config

    - name: Install Python dependencies (lean CI)
      working-directory: backend
      run: |
        python -m pip install --upgrade pip
        pip install --no-cache-dir ".[ci]"

    - name: Create .env file for tests
      working-directory: backend
      run: |
        echo "DATABASE_URL=${{ secrets.DATABASE_URL || 'postgresql://test' }}" >> .env
        echo "OLLAMA_BASE_URL=http://localhost:11434" >> .env
        echo "OLLAMA_MODEL=llama3.1:8b" >> .env
        echo "USE_LOCAL_EMBEDDINGS=true" >> .env
        echo "EMBEDDING_MODEL=sentence-transformers/all-MiniLM-L6-v2" >> .env
        echo "EMBEDDING_SERVICE_AUTH_TOKEN=test-embed-token" >> .env
        echo "EMBEDDING_STRICT_STARTUP=false" >> .env
        echo "REINDEX_TRIGGER_TOKEN=test-reindex-token" >> .env
        echo "REINDEX_SERVICE_URL=https://example.modal.run" >> .env
      shell: bash

    - name: Run backend integration tests (non-DB/non-LLM)
      working-directory: backend
      run: |
        pytest tests/ -m "integration and not db and not llm" -v --tb=short --junit-xml=integration-test-results.xml
      env:
        PYTHONPATH: ${{ github.workspace }}/backend
        EMBEDDING_STRICT_STARTUP: 'false'

  backend-schema:
    runs-on: ubuntu-22.04

    steps:
    - name: Checkout code
      uses: actions/checkout@v5
      with:
        submodules: 'false'

    - name: Set up Python 3.10
      uses: actions/setup-python@v6
      with:
        python-version: "3.10"
        cache: 'pip'

    - name: Install Python dependencies (lean CI)
      working-directory: backend
      run: |
        python -m pip install --upgrade pip
        pip install --no-cache-dir ".[ci]"

    - name: Create .env file for schema tests
      working-directory: backend
      run: |
        echo "DATABASE_URL=${{ secrets.DATABASE_URL || 'postgresql://test' }}" >> .env
        echo "OLLAMA_BASE_URL=http://localhost:11434" >> .env
        echo "OLLAMA_MODEL=llama3.1:8b" >> .env
        echo "USE_LOCAL_EMBEDDINGS=true" >> .env
        echo "EMBEDDING_MODEL=sentence-transformers/all-MiniLM-L6-v2" >> .env
        echo "EMBEDDING_SERVICE_AUTH_TOKEN=test-embed-token" >> .env
        echo "EMBEDDING_STRICT_STARTUP=false" >> .env
        echo "REINDEX_TRIGGER_TOKEN=test-reindex-token" >> .env
        echo "REINDEX_SERVICE_URL=https://example.modal.run" >> .env

    - name: Run backend schema tests
      working-directory: backend
      env:
        PYTHONPATH: ${{ github.workspace }}/backend
        EMBEDDING_STRICT_STARTUP: 'false'
        SCHEMATHESIS_HOOKS: tests.schemathesis_hooks
      run: |
        pytest tests/integration/test_api_schema_schemathesis.py -q --junit-xml=schema-test-results.xml

    - name: Upload backend schema artifacts
      if: always()
      uses: actions/upload-artifact@v4
      with:
        name: backend-schema-artifacts
        path: |
          backend/schema-test-results.xml
          backend/schema-coverage.html
        if-no-files-found: warn

  frontend-unit:
    runs-on: ubuntu-22.04

    steps:
    - name: Checkout code
      uses: actions/checkout@v5
      with:
        submodules: 'false'

    - name: Checkout frontend submodule
      run: git submodule update --init --depth 1 frontend

    - name: Set up Node.js
      uses: actions/setup-node@v5
      with:
        node-version: '20'
        cache: 'npm'
        cache-dependency-path: frontend/package-lock.json

    - name: Install frontend dependencies
      working-directory: frontend
      run: npm ci

    - name: Run frontend unit tests
      working-directory: frontend
      run: npm run test:coverage:unit

  frontend-e2e:
    runs-on: ubuntu-22.04
    timeout-minutes: 60
    strategy:
      fail-fast: false
      matrix:
        browser: [chromium, firefox]
        shardIndex: [1, 2, 3, 4, 5, 6]
        shardTotal: [6]

    steps:
    - name: Checkout code
      uses: actions/checkout@v5
      with:
        submodules: 'false'

    - name: Checkout frontend submodule
      run: git submodule update --init --depth 1 frontend

    - name: Set up Node.js
      uses: actions/setup-node@v5
      with:
        node-version: '20'
        cache: 'npm'
        cache-dependency-path: frontend/package-lock.json

    - name: Install frontend dependencies
      working-directory: frontend
      run: npm ci

    - name: Install Playwright browser
      working-directory: frontend
      run: npx playwright install --with-deps ${{ matrix.browser }}

    - name: Start compose stack for e2e
      run: |
        set +e
        docker compose -f docker-compose.dev.yml up -d frontend vecinita-gateway
        status=$?
        set -e
        if [ "$status" -ne 0 ]; then
          echo "First startup attempt failed, dumping logs and retrying once..."
          docker compose -f docker-compose.dev.yml ps || true
          docker compose -f docker-compose.dev.yml logs --no-color || true
          docker compose -f docker-compose.dev.yml down -v || true
          sleep 10
          docker compose -f docker-compose.dev.yml up -d frontend vecinita-gateway
        fi

    - name: Wait for frontend and gateway
      run: |
        for i in {1..40}; do
          curl -fsS http://127.0.0.1:15173 >/dev/null && curl -fsS http://127.0.0.1:18004/api/v1/ask/config >/dev/null && exit 0
          sleep 3
        done
        echo "Services did not become ready in time"
        docker compose -f docker-compose.dev.yml ps
        exit 1

    - name: Run frontend e2e shard
      working-directory: frontend
      env:
        CI: 'true'
        E2E_SKIP_WEBSERVER: 'true'
        E2E_BASE_URL: http://127.0.0.1:15173
      run: |
        npx playwright test \
          --project=${{ matrix.browser }} \
          --shard=${{ matrix.shardIndex }}/${{ matrix.shardTotal }}

    - name: Upload Playwright artifacts on failure
      if: failure() && !cancelled()
      uses: actions/upload-artifact@v4
      with:
        name: playwright-failure-${{ matrix.browser }}-shard-${{ matrix.shardIndex }}
        path: |
          frontend/playwright-report/
          frontend/test-results/
        retention-days: 7
        if-no-files-found: ignore

    - name: Dump compose logs on failure
      if: failure()
      run: docker compose -f docker-compose.dev.yml logs --no-color

    - name: Stop compose stack
      if: always()
      run: docker compose -f docker-compose.dev.yml down -v

  # ---------------------------------------------------------------------------
  # pgvector integration gate
  # Spins up a real Postgres 16 + pgvector container, runs DB migrations, then
  # executes every test marked `integration and db` (excluding LLM calls).
  # This closes the gap between mocked unit tests and a live Render Postgres DB.
  # ---------------------------------------------------------------------------
  backend-integration-pgvector:
    runs-on: ubuntu-22.04

    services:
      postgres:
        image: pgvector/pgvector:pg16
        env:
          POSTGRES_USER: vecinita
          POSTGRES_PASSWORD: vecinita
          POSTGRES_DB: vecinita_test
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    env:
      DATABASE_URL: postgresql://vecinita:vecinita@localhost:5432/vecinita_test

    steps:
      - name: Checkout code
        uses: actions/checkout@v5
        with:
          submodules: 'false'

      - name: Set up Python 3.10
        uses: actions/setup-python@v6
        with:
          python-version: '3.10'
          cache: 'pip'

      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends \
            build-essential graphviz graphviz-dev pkg-config postgresql-client

      - name: Install Python dependencies (lean CI)
        working-directory: backend
        run: |
          python -m pip install --upgrade pip
          pip install --no-cache-dir ".[ci]"

      - name: Run DB migrations
        run: |
          bash backend/scripts/run_migrations.sh

      - name: Verify pgvector and schema
        run: |
          psql "$DATABASE_URL" -c "SELECT '[1,2,3]'::vector;" -c "\dt"

      - name: Create .env file for tests
        working-directory: backend
        run: |
          echo "DATABASE_URL=${DATABASE_URL}" >> .env
          echo "OLLAMA_BASE_URL=http://localhost:11434" >> .env
          echo "OLLAMA_MODEL=llama3.1:8b" >> .env
          echo "USE_LOCAL_EMBEDDINGS=true" >> .env
          echo "EMBEDDING_MODEL=sentence-transformers/all-MiniLM-L6-v2" >> .env
          echo "EMBEDDING_SERVICE_AUTH_TOKEN=test-embed-token" >> .env
          echo "EMBEDDING_STRICT_STARTUP=false" >> .env
          echo "REINDEX_TRIGGER_TOKEN=test-reindex-token" >> .env
          echo "REINDEX_SERVICE_URL=https://example.modal.run" >> .env

      - name: Run integration tests against pgvector DB
        working-directory: backend
        env:
          PYTHONPATH: ${{ github.workspace }}/backend
          EMBEDDING_STRICT_STARTUP: 'false'
        run: |
          set +e
          pytest tests/ -m "integration and db and not llm" -v --tb=short \
            --junit-xml=integration-pgvector-results.xml
          status=$?
          set -e
          if [ "$status" -eq 5 ]; then
            echo "No backend integration tests are currently marked with both 'integration' and 'db'; treating this as a no-op after successful schema verification."
            exit 0
          fi
          exit "$status"

